To run the project
PRE-REQUIREMENTS:

-node

-typescript

-mongo db (user hard coded local config)

STEPS:

>>goto the project folder.

>>insert the command npm i to install node modules.

>>insert the command npm run server to start the server.

>>goto localhost:4000/docs to see open api swagger documentation.